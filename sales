#include <iostream>
#include <fstream>
#include <string>
using namespace std;


class Sale{

}



int main () {
vector <string> f1le;
  string line;
  ifstream myfile ("example.txt");    //reads file in as input and saves as a vector 
  if (myfile.is_open())
  {
    while ( getline (myfile,line) )
    {
      f1le.pushback(line) ;
    }
    myfile.close();
  }

 

  else cout << "Unable to open file"; 

 void puchase(string productName, int quant, string client){}
voud updateSpecSale(int quant, string service)
{

}

vector annualSales(){
cout<<f1le<<endl;
return f1le;
}
  return 0;
}
