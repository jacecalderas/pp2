#include <iostream>
#include <fstream>
#include <string>
using namespace std;

 

int main () {
vector <string> f1le;
  string line;
  ifstream myfile ("example.txt");
  if (myfile.is_open())
  {
    while ( getline (myfile,line) )
    {
      f1le.pushback(line) ;
    }
    myfile.close();
  }

 

  else cout << "Unable to open file"; 

 

  return 0;
}
